<?xml version="1.0" encoding="utf-8"?><!--
  ~ Copyright © All Contributors. See LICENSE and AUTHORS in the root directory for details.
  -->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:installLocation="internalOnly">

    <permission
        android:name="at.bitfire.davdroid.mudita.permission.TRIGGER_SYNC"
        android:protectionLevel="signature" />

    <!-- Signature-level permission required to read the auth (token) state, see KompaktAuthState -->
    <permission
        android:name="at.bitfire.davdroid.mudita.permission.READ_AUTH_STATE"
        android:protectionLevel="signature" />

    <!-- Signature-level permission required to request logout (account removal), see KompaktLogoutRequestReceiver -->
    <permission
        android:name="com.davx5.ose.permission.LOGOUT"
        android:protectionLevel="signature" />
    <!-- Held by this app so it can send the permission-protected AUTH_STATE_CHANGED broadcast -->
    <uses-permission android:name="at.bitfire.davdroid.mudita.permission.READ_AUTH_STATE" />

    <application android:name=".App"
            android:icon="@mipmap/ic_launcher">

        <!-- Remove the launcher intent filter from the core AccountsActivity -->
        <activity
            android:name="at.bitfire.davdroid.ui.AccountsActivity"
                android:exported="true"
                tools:node="merge">
            <intent-filter tools:node="remove">
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Kompakt launcher replaces the core AccountsActivity as the entry point -->
        <activity
            android:name="at.bitfire.davdroid.ui.KompaktAccountsActivity"
            android:exported="true"
            android:theme="@style/Theme.Kompakt.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Kompakt login flow: returns a result instead of opening the upstream AccountActivity -->
        <activity
            android:name="at.bitfire.davdroid.ui.setup.KompaktLoginActivity"
            android:parentActivityName="at.bitfire.davdroid.ui.KompaktAccountsActivity"
            android:windowSoftInputMode="adjustResize"
            android:exported="false" />

        <!-- Kompakt OAuth: runs the Google authorization in an embedded WebView (no Chrome/Custom Tabs on device) -->
        <activity
            android:name="at.bitfire.davdroid.ui.setup.KompaktOAuthWebViewActivity"
            android:exported="false" />

        <!-- Required for Hilt/WorkManager integration. See
        - https://developer.android.com/develop/background-work/background-tasks/persistent/configuration/custom-configuration#remove-default
        - https://developer.android.com/training/dependency-injection/hilt-jetpack#workmanager
        However, we must not disable AndroidX startup completely, as it's needed by other libraries like okhttp. -->
        <provider
                android:name="androidx.startup.InitializationProvider"
                android:authorities="${applicationId}.androidx-startup"
                android:exported="false"
                tools:node="merge">
            <meta-data
                    android:name="androidx.work.WorkManagerInitializer"
                    android:value="androidx.startup"
                    tools:node="remove" />
        </provider>

        <provider
                android:name="at.bitfire.davdroid.webdav.DavDocumentsProvider"
                android:authorities="${webdavAuthority}"
                android:exported="true"
                android:grantUriPermissions="true"
                android:permission="android.permission.MANAGE_DOCUMENTS">
            <intent-filter>
                <action android:name="android.content.action.DOCUMENTS_PROVIDER" />
            </intent-filter>
        </provider>

        <receiver
            android:name="at.bitfire.davdroid.ui.KompaktSyncRequestReceiver"
            android:exported="true"
            android:permission="at.bitfire.davdroid.mudita.permission.TRIGGER_SYNC">
            <intent-filter>
                <action android:name="at.bitfire.davdroid.mudita.action.REQUEST_SYNC" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="at.bitfire.davdroid.ui.KompaktLogoutRequestReceiver"
            android:exported="true"
            android:permission="com.davx5.ose.permission.LOGOUT">
            <intent-filter>
                <action android:name="com.davx5.ose.action.LOGOUT" />
            </intent-filter>
        </receiver>

        <!-- Exposes the per-account auth (token) state to same-signed apps, see KompaktAuthState -->
        <provider
            android:name="at.bitfire.davdroid.ui.KompaktAuthStateProvider"
            android:authorities="at.bitfire.davdroid.mudita.kompakt.authstate"
            android:exported="true"
            android:readPermission="at.bitfire.davdroid.mudita.permission.READ_AUTH_STATE" />

        <!-- provider to share debug info/logs -->
        <provider
                android:name="androidx.core.content.FileProvider"
                android:authorities="${debugInfoAuthority}"
                android:grantUriPermissions="true"
                android:exported="false">
            <meta-data
                    android:name="android.support.FILE_PROVIDER_PATHS"
                    android:resource="@xml/debug_paths" />
        </provider>

    </application>

</manifest>